pypdf>=4.2.0,<6.0.0

# Optional dependencies for OCR fallback (scanned PDFs without embedded text).
# The system tools Tesseract and Poppler are also required:
#   brew install tesseract tesseract-lang poppler
pytesseract>=0.3.10
pdf2image>=1.17.0
Pillow>=10.0.0
